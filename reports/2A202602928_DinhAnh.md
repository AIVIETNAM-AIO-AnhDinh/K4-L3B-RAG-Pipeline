# Individual contribution report

---

## Thông tin

- Họ và tên: Đinh Lệnh Tiến Anh
- Mã học viên: 2A202602928
- Nhóm: 5changlinhngulam
- Repository/branch: (https://github.com/AIVIETNAM-AIO-AnhDinh/K4-L3B-RAG-Pipeline_5changlinhngulam)

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 1 — Thu thập tài liệu chính sách | Chọn đề tài "Chính sách người mua/người bán trên Shopee VN", lập `sources.csv` (5 chính sách công khai từ help.shopee.vn), viết script render Markdown → PDF (fpdf2, font Unicode cho tiếng Việt) để giữ đúng pipeline landing (PDF/DOCX) → standardized (Markdown) | `src/task1_collect_legal_docs.py`, `data/landing/legal/sources.csv`, `data/landing/legal/*.pdf`, commit `234f73b` "anh: data" | Done |
| Task 2 — Crawl bài viết/thông báo | Chọn 7 bài FAQ/hướng dẫn trên Trung tâm trợ giúp Shopee (trả hàng, hoàn tiền, vận chuyển) bổ sung cho Task 1; viết crawler bất đồng bộ dùng Crawl4AI + Playwright (headless, chờ `networkidle`), hàm `extract_article` cắt bỏ menu điều hướng và widget đánh giá, lưu mỗi bài thành JSON | `src/task2_crawl_news.py`, `data/landing/news/article_01.json`…`article_07.json`, commit `234f73b` | Done |
| Task 3 — Chuẩn hóa dữ liệu sang Markdown | Viết pipeline chuẩn hóa: legal (PDF → Markdown qua MarkItDown, gộp metadata từ `sources.csv` + frontmatter gốc), news (JSON → Markdown); xử lý loại bỏ boilerplate, unwrap văn bản PDF bị ngắt dòng, làm sạch markdown web (bỏ ảnh, link, bảng lỗi), chuẩn hóa Unicode NFC, sinh YAML frontmatter cho Task 4 đọc lại | `src/task3_convert_markdown.py`, `data/standardized/legal/*.md`, `data/standardized/news/*.md`, commit `234f73b` | Done |
| Fix pipeline evaluation | Sửa lỗi trong script evaluation và cập nhật generation liên quan tới kết quả retrieval | `group_project/evaluation/evaluate.py`, `src/task10_generation.py`, `group_project/evaluation/results/retrieval_results.json`, commit `88fe234` "fix eval" | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

1. **Quyết định:** Giữ nguyên kiến trúc pipeline landing (PDF/DOCX) → standardized (Markdown) cho Task 1, dù nguồn gốc (help.shopee.vn) là trang HTML không có PDF sẵn — nên chủ động render Markdown gốc thành PDF (fpdf2) rồi để Task 3 convert ngược lại bằng MarkItDown.
   **Lý do/evidence:** Đảm bảo pipeline nhất quán với các nguồn tài liệu khác trong nhóm (không tạo ngoại lệ riêng cho dữ liệu dạng HTML); xem `src/task1_collect_legal_docs.py` (docstring dòng 10-13) và `src/task3_convert_markdown.py::convert_legal_docs`.
   **Trade-off:** Tốn thêm một bước render/convert qua lại (PDF trung gian) so với việc giữ thẳng HTML/Markdown gốc; đổi lại corpus đồng nhất về định dạng nguồn và dễ tái tạo lại toàn bộ pipeline từ đầu.

2. **Quyết định:** Dùng Crawl4AI + Playwright (headless browser) thay vì requests/BeautifulSoup thông thường để crawl help.shopee.vn.
   **Lý do/evidence:** Trang help.shopee.vn render nội dung bằng JavaScript, cần đợi `networkidle` và `delay_before_return_html=3` mới lấy được nội dung đầy đủ; xem `src/task2_crawl_news.py::crawl_article`.
   **Trade-off:** Crawl chậm hơn và phụ thuộc cài đặt trình duyệt (`playwright install chromium`), nhưng là cách duy nhất lấy được nội dung đúng thay vì trang rỗng/loading.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: Chạy trực tiếp `python -m src.task1_collect_legal_docs`, `python -m src.task2_crawl_news`, `python -m src.task3_convert_markdown` để kiểm tra idempotency (chạy lại không tạo file trùng, ghi đè đúng file cùng tên); kiểm tra thủ công nội dung `data/standardized/legal/*.md` và `data/standardized/news/*.md` (frontmatter đầy đủ, không còn boilerplate/menu điều hướng).
- Kết quả trước/sau nếu có: Sau khi thêm `remove_boilerplate` và `extract_article`, các dòng menu điều hướng ("Xin chào, Shopee có thể giúp gì cho bạn?"), widget đánh giá cuối bài, và dòng nguồn trùng lặp đã bị loại khỏi Markdown chuẩn hóa; PDF chính sách unwrap đúng thành đoạn văn thay vì giữ ngắt dòng theo layout trang.
- Lỗi đã phát hiện và cách xử lý: (1) Một số bài crawl trả về nội dung < 200 ký tự do lỗi tải trang — xử lý bằng cách raise lỗi và log `Failed: {url}` để chạy lại thay vì lưu dữ liệu rỗng. (2) Trang web trả Unicode dạng tổ hợp (NFD) gây lỗi hiển thị dấu tiếng Việt — xử lý bằng `unicodedata.normalize("NFC", ...)` trong `write_markdown`. (3) Lỗi trong `evaluate.py` khiến kết quả retrieval/generation không khớp — đã sửa ở commit `88fe234`.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: `find_font()` trong Task 1 phụ thuộc vào font Unicode có sẵn trên máy (Arial Unicode/DejaVu Sans) theo đường dẫn cứng, nên script có thể lỗi `FileNotFoundError` trên môi trường không có sẵn các font này.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: Đóng gói/tải kèm một font Unicode cố định trong repo (thay vì dò trên hệ thống) để đảm bảo Task 1 chạy lại được trên mọi máy mà không cần cấu hình thêm.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 2026-09-25
- Tên thành viên: Đinh Lệnh Tiến Anh
